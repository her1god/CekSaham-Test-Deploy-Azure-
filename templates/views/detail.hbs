<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta content="width=device-width, initial-scale=1.0" name="viewport" />

  <title>Detail - CekSaham</title>
  <meta content="" name="description" />
  <meta content="" name="keywords" />

  <!-- Favicons -->
  <link href="/img/favicon.png" rel="icon" />
  <link href="/img/apple-touch-icon.png" rel="apple-touch-icon" />

  <!-- Google Fonts -->
  <link href="https://fonts.gstatic.com" rel="preconnect" />
  <link
    href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Nunito:300,300i,400,400i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i"
    rel="stylesheet" />

  <!-- Vendor CSS Files -->
  <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
  <link href="/vendor/bootstrap-icons/bootstrap-icons.css" rel="stylesheet" />
  <link href="/vendor/boxicons/css/boxicons.min.css" rel="stylesheet" />
  <link href="/vendor/quill/quill.snow.css" rel="stylesheet" />
  <link href="/vendor/quill/quill.bubble.css" rel="stylesheet" />
  <link href="/vendor/remixicon/remixicon.css" rel="stylesheet" />
  <link href="/vendor/simple-datatables/style.css" rel="stylesheet" />

  <!-- Template Main CSS File -->
  <link href="/css/style.css" rel="stylesheet" />
</head>
<script>
  // Auto append token ke semua link dan form
  document.addEventListener('DOMContentLoaded', function() {
    const token = '{{token}}';
    
    if (token) {
      // Append token ke semua link
      document.querySelectorAll('a').forEach(link => {
        const href = link.getAttribute('href');
        if (href && !href.includes('token=') && !href.startsWith('#')) {
          const separator = href.includes('?') ? '&' : '?';
          link.setAttribute('href', href + separator + 'token=' + token);
        }
      });
      
      // Append token ke semua form
      document.querySelectorAll('form').forEach(form => {
        const action = form.getAttribute('action');
        if (action && !action.includes('token=')) {
          const separator = action.includes('?') ? '&' : '?';
          form.setAttribute('action', action + separator + 'token=' + token);
        }
      });
    }
  });
</script>

<body>
  {{> messages}}

  <!-- ======= Header ======= -->
  <header id="header" class="header fixed-top d-flex align-items-center">

    <div class="d-flex align-items-center justify-content-between">
      <a href="/" class="logo d-flex align-items-center">
        <img src="/img/logo.png" alt="" />
        <span class="d-none d-lg-block">CekSaham</span>
      </a>
      <i class="bi bi-list toggle-sidebar-btn"></i>
    </div><!-- End Logo -->

    <div class="search-bar">
      <form class="search-form d-flex align-items-center" method="GET" action="/search?token={{token}}">
        <input type="text" name="search" placeholder="Cari dengan 4 kode saham" />
        <!-- Tambahkan elemen dropdown/select -->
        <select id="tipeCari" class="search-form d-flex align-items-center" name="tipeCari">
          <option value="company">Informasi Perusahaan</option>
          <option value="stock">Naik Turun Harga Saham</option>
        </select>
        <button type="submit"><i class="bi bi-search"></i></button>
      </form>
    </div><!-- End Search Bar -->

      <nav class="header-nav ms-auto">
          <span>Welcome, {{user.email}}</span>
          <a href="/logout">Logout</a>
      </nav><!-- End Icons Navigation -->

  </header><!-- End Header -->

  <!-- ======= Sidebar ======= -->
  <aside id="sidebar" class="sidebar">

    <ul class="sidebar-nav" id="sidebar-nav">

      <li class="nav-item">
        <a class="nav-link" href="/">
          <i class="bi bi-grid"></i>
          <span>Dashboard</span>
        </a>
      </li><!-- End Dashboard Nav -->
      <li class="nav-item">
        <a class="nav-link" href="hasilSimpan-stok">
          <i class="bi bi-grid"></i>
          <span>Check Saved Stocks</span>
        </a>
      </li><!-- End Dashboard Nav -->

    </ul>

  </aside><!-- End Sidebar-->

  <main id="main" class="main">

    <div class="pagetitle">
      <h1>Details</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="/">Home</a></li>
          <li class="breadcrumb-item active">Details</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <!-- Formulir untuk menyimpan saham -->
    <form action="/simpan-stok?token={{token}}" method="post">
      <input type="hidden" name="symbol" value="{{companyDetail.symbol}}" />
      <input type="hidden" name="name" value="{{companyDetail.name}}" />
      <input type="hidden" name="logo" value="{{companyDetail.logo}}" />
      <button class="btn btn-success" type="submit">Simpan Saham</button>
    </form>

    <!-- Tampilkan detail perusahaan -->
    <p>Name: {{companyDetail.name}}</p>
    <p>Symbol: {{companyDetail.symbol}}</p>
    <p>Alamat: {{companyDetail.address}}</p>
    <p>Telepon: {{companyDetail.phone}}</p>
    <p>Faks: {{companyDetail.fax}}</p>
    <p>NPWP: {{companyDetail.npwp}}</p>
    <p>Email: {{companyDetail.email}}</p>
    <p>Website: <a href="{{companyDetail.website}}" target="_blank">{{companyDetail.website}}</a></p>
    <p>Sektor: {{companyDetail.sector_name}}</p>
    <p>Sub Sektor: {{companyDetail.sub_sector_name}}</p>
    <p>Industri: {{companyDetail.industry_name}}</p>
    <p>Sub Industri: {{companyDetail.sub_industry_name}}</p>

    <p>About: {{companyDetail.about}}</p>

    <a href="/">Kembali ke Daftar Perusahaan</a>
  </main><!-- End #main -->

  <!-- ======= Footer ======= -->
  <footer id="footer" class="footer">
    <div class="copyright">
      &copy; Copyright
      <strong><span>CekSaham</span></strong>. All Rights Reserved
    </div>
    <div class="credits">
      <!-- All the links in the footer should remain intact. -->
      <!-- You can delete the links only if you purchased the pro version. -->
      <!-- Licensing information: https://bootstrapmade.com/license/ -->
      <!-- Purchase the pro version with working PHP/AJAX contact form: https://bootstrapmade.com/nice-admin-bootstrap-admin-html-template/ -->
      Designed by
      <a href="https://bootstrapmade.com/">her1god</a>
    </div>
  </footer><!-- End Footer -->

  <a href="#" class="back-to-top d-flex align-items-center justify-content-center"><i
      class="bi bi-arrow-up-short"></i></a>

  <!-- Vendor JS Files -->
  <script src="/vendor/apexcharts/apexcharts.min.js"></script>
  <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
  <script src="/vendor/chart.js/chart.umd.js"></script>
  <script src="/vendor/echarts/echarts.min.js"></script>
  <script src="/vendor/quill/quill.min.js"></script>
  <script src="/vendor/simple-datatables/simple-datatables.js"></script>
  <script src="/vendor/tinymce/tinymce.min.js"></script>
  <script src="/vendor/php-email-form/validate.js"></script>

  <!-- Template Main JS File -->
  <script src="/js/main.js"></script>

</body>

</html>